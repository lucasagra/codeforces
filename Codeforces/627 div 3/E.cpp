#include <bits/stdc++.h>
using namespace std;
typedef long long ll;

int main(){
    int n, h, l, r; cin >> n >> h >> l >> r;
    int hours[n];
    for(int i = 0; i < n; i ++) {
        cin >> hours[i];
    }

    
    return 0;
}